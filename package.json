{
  "name": "up-for-a-ride",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/web",
    "apps/worker"
  ],
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply up_for_a_ride_db"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251115.0"
  }
}
